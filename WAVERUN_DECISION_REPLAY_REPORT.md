# WAVERUN Decision replay

REPLAY DETERMINISM: NOT RUN

The updated records contain the complete causal input for 508 evaluations generated after provenance integration. A replay harness is not yet present in the canonical repository, so no PASS claim is made. The earlier 1,897-record session predates causal-input persistence and cannot be replayed faithfully from output-only records.

This is an explicit remaining acceptance item, not a fabricated deterministic result.
